// src/types/index.ts

import * as API from './api';
import * as Models from './models';
import * as Shared from './Shared';

export {
  API,
  Models,
  Shared
};

const Types = {
  API,
  Models,
  Shared,
};

export default Types;
